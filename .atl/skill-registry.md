# Skill Registry — splitwise

<!-- Auto-generated by sdd-init. Run `skill-registry` skill to regenerate. -->

Last updated: 2026-06-05

## Sources scanned

- C:\Users\PODER JUDICIAL\.config\opencode\skills
- C:\Users\PODER JUDICIAL\.agents\skills
- C:\Users\PODER JUDICIAL\.gemini\skills
- C:\Users\PODER JUDICIAL\.gemini\antigravity\skills
- C:\Users\PODER JUDICIAL\.cursor\skills

## Contract

**Delegator use only.** Any agent that launches subagents reads this registry to resolve compact rules, then injects matching rule text into subagent prompts under `## Project Standards (auto-resolved)`.

Subagents still read their assigned executor/phase skill. During normal runtime, they do **not** independently discover or load additional project/user `SKILL.md` files or this registry; project/user rules arrive pre-digested. Explicit fallback loading is degraded self-healing and must be reported in `skill_resolution` as `fallback-registry` or `fallback-path`.

## Selected skills and compact rules

### branch-pr
- Path: C:\Users\PODER JUDICIAL\.config\opencode\skills\branch-pr\SKILL.md
- Trigger: Create Gentle AI pull requests with issue-first checks. Trigger: creating, opening, or preparing PRs for review.
- Rules:
  - **Every PR MUST link an approved issue** — no exceptions
  - **Every PR MUST have exactly one `type:*` label**
  - **Automated checks must pass** before merge is possible
  - **Blank PRs without issue linkage will be blocked** by GitHub Actions
  - Branch names MUST match `^(feat|fix|chore|docs|style|refactor|perf|test|build|ci|revert)\/[a-z0-9._-]+$`
  - Use conventional commits

### chained-pr
- Path: C:\Users\PODER JUDICIAL\.config\opencode\skills\chained-pr\SKILL.md
- Trigger: PRs over 400 lines, stacked PRs, review slices. Split oversized changes into chained PRs that protect review focus.
- Rules:
  - Split PRs over **400 changed lines** unless a maintainer explicitly accepts `size:exception`
  - Keep each PR reviewable in about **≤60 minutes**
  - Use one deliverable work unit per PR; keep tests/docs with the unit they verify
  - State start, end, prior dependencies, follow-up work, and out-of-scope items in every chained PR
  - Every child PR must include a dependency diagram marking the current PR with `📍`
  - In Feature Branch Chain, create a draft/no-merge tracker PR; child PR #1 targets the tracker branch, later children target the immediate parent branch
  - Treat polluted diffs as base bugs: retarget or rebase until only the current work unit appears
  - Do not mix chain strategies after the user chooses one
  - Condition → Action: PR ≤400 lines → keep single PR; PR >400, independent slices → Stacked PRs; PR >400, must integrate before main → Feature Branch Chain; generated/vendor/migration diff → ask for `size:exception`; SDD provides `delivery_strategy` → follow it

### cognitive-doc-design
- Path: C:\Users\PODER JUDICIAL\.config\opencode\skills\cognitive-doc-design\SKILL.md
- Trigger: Design docs that reduce cognitive load. Trigger: writing guides, READMEs, RFCs, onboarding, architecture, or review-facing docs.
- Rules:
  - Lead with the answer: put decision/action/outcome first, context comes after
  - Progressive disclosure: start with happy path, then details, edge cases, references
  - Chunking: group related info into small sections; keep flat lists short
  - Signposting: use headings, labels, callouts, summaries so readers know where they are
  - Recognition over recall: prefer tables, checklists, examples, templates over prose
  - Review empathy: design docs so reviewers can verify intent without reconstructing the whole story

### comment-writer
- Path: C:\Users\PODER JUDICIAL\.config\opencode\skills\comment-writer\SKILL.md
- Trigger: Write warm, direct collaboration comments. Trigger: PR feedback, issue replies, reviews, Slack messages, or GitHub comments.
- Rules:
  - Be useful fast: start with actionable point; do not recap the whole PR
  - Be warm and direct: sound like a thoughtful teammate, not a corporate bot
  - Keep it short: 1-3 paragraphs or a tight bullet list
  - Explain why: give the technical reason when asking for a change
  - Avoid pile-ons: comment on highest-value issue, not every tiny preference
  - Match thread language; if Spanish, use Rioplatense voseo: `podés`, `tenés`, `fijate`, `dale`
  - No em dashes: use commas, periods, or parentheses instead

### engram-sync
- Path: C:\Users\PODER JUDICIAL\.config\opencode\skills\engram-sync\SKILL.md
- Trigger: Sync engram memories to GitHub. Trigger: user asks to sync, backup, or push memories to GitHub; session closing with pending memories.
- Rules:
  - Run `engram sync` in `~/.engram/` to export memories
  - If new memories exported, `git add observations/ sessions/ && git commit && git push`
  - If `engram sync` fails, do NOT commit
  - Remote: https://github.com/MaximilianoVallejo1991/engram.git
  - SQLite DB (`engram.db`) is NOT committed — only exported chunks
  - Sync is idempotent: running twice with no changes produces no output

### find-skills
- Path: C:\Users\PODER JUDICIAL\.agents\skills\find-skills\SKILL.md
- Trigger: Helps users discover and install agent skills when they ask how to do something, find a skill, or extend capabilities.
- Rules:
  - Use `npx skills find [query]` to search for skills interactively or by keyword
  - Use `npx skills add <package>` to install a skill from GitHub or other sources
  - Use `npx skills check` to check for updates; `npx skills update` to update all
  - Skills are modular packages extending agent capabilities with specialized knowledge and workflows

### gemini-api-dev
- Path: C:\Users\PODER JUDICIAL\.agents\skills\gemini-api-dev\SKILL.md
- Trigger: Building apps with Gemini models, multimodal content, function calling, structured outputs. Covers SDKs for Python, JS/TS, Java, Go.
- Rules:
  - Current models: `gemini-3-pro-preview` (1M tokens), `gemini-3-flash-preview` (1M), `gemini-3-pro-image-preview`
  - Models like `gemini-2.5-*`, `gemini-2.0-*`, `gemini-1.5-*` are legacy and deprecated
  - SDKs: `google-genai` (Python), `@google/genai` (JS/TS), `com.google.genai:google-genai` (Java), `google.golang.org/genai` (Go)
  - Supports text generation, multimodal understanding, function calling, structured output, code execution, context caching, embeddings

### go-testing
- Path: C:\Users\PODER JUDICIAL\.config\opencode\skills\go-testing\SKILL.md
- Trigger: Go tests, go test coverage, Bubbletea teatest, golden files. Apply focused Go testing patterns.
- Rules:
  - Prefer table-driven tests with `t.Run(tt.name, ...)` for multiple cases
  - Test behavior/state transitions, not implementation trivia
  - Use `t.TempDir()` for filesystem tests; never rely on real home directory
  - Keep integration tests skippable with `testing.Short()` when running external commands
  - For Bubbletea: test `Model.Update()` directly for state; use `teatest` only for interactive flows
  - Golden files: deterministic only; update via repo's `-update` path then rerun without `-update`
  - Use small mocks/interfaces around system boundaries
  - Condition → Pattern: pure function/parser → table-driven unit; error behavior → explicit success/failure cases; file operations → `t.TempDir()` assertions; TUI state → `Model.Update()`; full TUI → `teatest`; rendered output → golden file; real command → integration test with `-short`

### issue-creation
- Path: C:\Users\PODER JUDICIAL\.config\opencode\skills\issue-creation\SKILL.md
- Trigger: Create GitHub issues, bug reports, or feature requests with issue-first checks.
- Rules:
  - **Blank issues are disabled** — MUST use a template (bug report or feature request)
  - **Every issue gets `status:needs-review` automatically** on creation
  - **A maintainer MUST add `status:approved`** before any PR can be opened
  - **Questions go to Discussions**, not issues
  - Bug report template: `.github/ISSUE_TEMPLATE/bug_report.yml`, labels: `bug`, `status:needs-review`
  - Feature request template: `.github/ISSUE_TEMPLATE/feature_request.yml`, labels: `enhancement`, `status:needs-review`

### judgment-day
- Path: C:\Users\PODER JUDICIAL\.config\opencode\skills\judgment-day\SKILL.md
- Trigger: judgment day, dual review, adversarial review, juzgar. Run blind dual review, fix confirmed issues, then re-judge.
- Rules:
  - Resolve project skills before launching agents: read registry, match compact rules, inject `Project Standards` into both judge and fix prompts
  - Launch **two blind judges in parallel** with identical target/criteria; never review code yourself
  - Wait for both judges before synthesis; never accept partial verdict
  - Classify warnings as `WARNING (real)` only if normal intended use triggers them; otherwise `INFO` as `WARNING (theoretical)`
  - Ask before fixing Round 1 confirmed issues
  - After any fix agent runs, immediately re-launch both judges before commit/push/done
  - Terminal states: only `JUDGMENT: APPROVED` or `JUDGMENT: ESCALATED`
  - After 2 fix iterations with remaining issues, ask user whether to continue
  - Condition → Action: unclear target → ask for scope; no registry → warn and proceed; both judges agree → confirmed (fix); one judge finds issue → suspect, report, don't auto-fix; judges contradict → escalate; Round 2+ has only theory/suggestions → report as INFO

### skill-creator
- Path: C:\Users\PODER JUDICIAL\.config\opencode\skills\skill-creator\SKILL.md
- Trigger: New skills, agent instructions, documenting AI usage patterns. Create LLM-first skills with valid frontmatter.
- Rules:
  - A skill is a runtime instruction contract for an LLM, not human documentation
  - `description` MUST be one physical line, quoted, YAML-safe, include trigger words first; ≤250 chars
  - Frontmatter MUST include: `name`, `description`, `license`, `metadata.author`, `metadata.version`
  - Use imperative instructions, not tutorials or background prose
  - Keep body concise: target 180-450 tokens, hard max 1000
  - Code templates/schemas → `assets/`; conceptual detail/existing docs → local links in `references/`
  - Section order: Activation Contract, Hard Rules, Decision Gates, Execution Steps, Output Contract, References

### work-unit-commits
- Path: C:\Users\PODER JUDICIAL\.config\opencode\skills\work-unit-commits\SKILL.md
- Trigger: Plan commits as reviewable work units. Trigger: implementation, commit splitting, chained PRs, or keeping tests and docs with code.
- Rules:
  - Commit by work unit: a commit = deliverable behavior, fix, migration, or docs unit
  - Do NOT commit by file type: avoid `models` → `services` → `tests` if none works alone
  - Keep tests with code: tests belong in same commit as the behavior they verify
  - Keep docs with the user-visible change: docs belong with the feature/workflow they explain
  - Tell a story: reviewer should understand why each commit exists from diff + message
  - Future PR-ready: each commit should be a candidate chained PR when the change grows
  - SDD workload guard: if tasks forecast >400-line change, group commits into chained PR slices before implementation
